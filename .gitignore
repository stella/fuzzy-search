# Dependencies
node_modules

# Build outputs
target
dist
*.node
*.wasm

# Platform packages (binaries generated by napi artifacts)
npm/*/*.node

# WASM sub-package binary (committed for browser builds)
!npm/wasm32-wasi/fuzzy-search.wasm32-wasi.wasm

# Env
.env
.env.*

# Debug
npm-debug.log*

# Benchmark corpus (downloaded separately)
__bench__/corpus/*.txt
__bench__/corpus/E.coli
__bench__/corpus/*.tar.gz

# Bench dependencies (separate from root)
__bench__/node_modules
__bench__/bun.lock

# Misc
.DS_Store
*.pem

# Turbo / cache
.turbo
.cache

# Lock files (text-based bun.lock is committed)
bun.lockb
